layer:
  id: landcover_line
  buffer_size: 20
  fields:
    area:
      description: area
    name:
      description: name
    natural:
      description: natural
    osm_id:
      description: osm_id
    type:
      description: type
    way:
      description: way
    way_area:
      description: way_area
    website:
      description: website
    wikipedia:
      description: wikipedia
    wood:
      description: wood
    z_order:
      description: z_order
  datasource:
    geometry_field: way
    query: (SELECT FROM ) AS t
schema:
- landcover_line.sql
datasources:
- type: imposm3
  mapping_file: ./mapping.yaml
