layer:
  id: pistearea
  buffer_size: 20
  fields:
    area:
      description: area
    bridge:
      description: bridge
    colour:
      description: colour
    description:
      description: description
    distance:
      description: distance
    layer:
      description: layer
    name:
      description: name
    network:
      description: network
    operator:
      description: operator
    osm_id:
      description: osm_id
    osmcsymbol:
      description: osmcsymbol
    piste:difficulty:
      description: piste:difficulty
    piste:grooming:
      description: piste:grooming
    piste:name:
      description: piste:name
    piste:type:
      description: piste:type
    ref:
      description: ref
    symbol:
      description: symbol
    tunnel:
      description: tunnel
    way:
      description: way
    way_area:
      description: way_area
    website:
      description: website
    z_order:
      description: z_order
  datasource:
    geometry_field: way
    query: (SELECT FROM ) AS t
schema:
- pistearea.sql
datasources:
- type: imposm3
  mapping_file: ./mapping.yaml
