layers:
  id: aeroway
  buffer_size: 20
  fields:
    aeroway:
      description: aeroway
    area:
      description: area
    bridge:
      description: bridge
    layer:
      description: layer
    name:
      description: name
    osm_id:
      description: osm_id
    tunnel:
      description: tunnel
    way:
      description: way
    way_area:
      description: way_area
    z_order:
      description: z_order
  datasource:
    geometry_field: way
    query: (SELECT FROM ) AS t
schema:
- aeroway.sql
datasources:
- type: imposm3
  mapping_file: ./mapping.yaml
