layers:
  id: peaks
  buffer_size: 20
  fields:
    area:
      description: area
    ele:
      description: ele
    int_name:
      description: int_name
    name:
      description: name
    name:cs:
      description: name:cs
    name:de:
      description: name:de
    name:en:
      description: name:en
    natural:
      description: natural
    osm_id:
      description: osm_id
    way:
      description: way
    z_order:
      description: z_order
  datasource:
    geometry_field: way
    query: (SELECT FROM ) AS t
schema:
- peaks.sql
datasources:
- type: imposm3
  mapping_file: ./mapping.yaml
