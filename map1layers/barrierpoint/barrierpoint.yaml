layers:
  id: barrierpoint
  buffer_size: 20
  fields:
    area:
      description: area
    barrier:
      description: barrier
    bridge:
      description: bridge
    layer:
      description: layer
    name:
      description: name
    osm_id:
      description: osm_id
    tunnel:
      description: tunnel
    way:
      description: way
    z_order:
      description: z_order
  datasource:
    geometry_field: way
    query: (SELECT FROM ) AS t
schema:
- barrierpoint.sql
datasources:
- type: imposm3
  mapping_file: ./mapping.yaml
